# Transitive via @orion-finance/protocol → @uniswap/v3-periphery → @openzeppelin/contracts@3.4.2-solc-0.7
# Neither protocol nor protocol-plugins import or deploy that package (only Uniswap interfaces; runtime uses on-chain router/quoter).
# Direct OZ dependency is @openzeppelin/contracts@5.6.1.
vulnerabilities:
  # FP: 3.4.2-solc-0.7 is the patched release for TimelockController (GHSA-fg47-3c2x-m2wr).
  - id: CVE-2021-39167
    expired_at: 2027-03-10
    purls:
      - "pkg:npm/%40openzeppelin/contracts@3.4.2-solc-0.7"
    statement: Transitive Uniswap OZ 0.7 sources; TimelockController patched in this release; not used by Orion.